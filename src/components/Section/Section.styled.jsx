import styled from 'styled-components';

const Section = styled.section`
  margin: 0 auto;
  margin-left: 40px;
  text-align: left;
  font-size: 18px;
  color: rgb(73, 52, 116);
}
`;

const style = {
  Section,
};

export default style;